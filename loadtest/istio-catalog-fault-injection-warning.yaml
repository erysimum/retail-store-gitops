apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: catalog-fault-injection
  namespace: catalog-dev
  labels:
    app.kubernetes.io/part-of: retail-store
    app.kubernetes.io/component: chaos-engineering
    chaos.retail-store/tier: warning
spec:
  hosts:
    - catalog
  http:
    - fault:
        abort:
          percentage:
            value: 0.5         # 0.5% slow burn -> warning tier
          httpStatus: 500
      route:
        - destination:
            host: catalog